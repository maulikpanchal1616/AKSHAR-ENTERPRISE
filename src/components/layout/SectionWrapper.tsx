// SectionWrapper stub — replaced by direct section tags in page.tsx
export default function SectionWrapper({ children }: { children: React.ReactNode; id?: string; noBorder?: boolean; className?: string }) {
  return <>{children}</>;
}
import React from 'react';
